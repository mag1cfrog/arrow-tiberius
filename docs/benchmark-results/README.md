# Benchmark Results

This directory contains selected local benchmark result notes that are useful
for tracking development decisions.

These files are not a public performance guarantee. Each result should describe
the machine, runtime, SQL Server image, Rust version, exact command, and caveats
needed to interpret the numbers.
